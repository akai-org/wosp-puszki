import { Main } from '../../components/main';

// needed:
// separate component with all
// inside that separated component for collected cause it will have api requests
const MainPage: React.FC = () => <Main />;

export default MainPage;
